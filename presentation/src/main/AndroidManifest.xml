<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_cagaub"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_cagaub_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.CagaUB">
        <activity
            android:name=".ui.activities.ToiletInfoActivity"
            android:exported="false" />
        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBxFxz8ErdroMciRwTC-sQ2t_gYZkXjn0A" />

        <activity
            android:name=".ui.activities.MainActivity"
            android:exported="true"
            android:label="MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activities.AddToiletActivity"
            android:exported="false"
            android:label="AddToiletActivity" />
        <activity
            android:name=".ui.activities.LogInActivity"
            android:exported="false"
            android:label="LogInActivity" />
        <activity
            android:name=".ui.activities.SignUpActivity"
            android:exported="false"
            android:label="SignUpActivity" />

        <activity
            android:name=".ui.activities.AddValorationActivity"
            android:exported="false"
            android:label="AddValorationActivity" />
    </application>

</manifest>